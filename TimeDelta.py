from datetime import timedelta

#class time_delta():
    #def function(self):
        #print(timedelta(days=365, hours=5, minutes=20, seconds=20))

print(timedelta(days=365, hours=5, minutes=20, seconds=20))